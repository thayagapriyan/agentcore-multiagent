{
  "name": "@multiagent/common",
  "version": "0.1.0",
  "description": "Shared agent runtime: Express /ping+/invocations wrapper, Bedrock model factory, shared types. Imported by every agent deployable.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "express": "^4.19.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "rimraf": "^5.0.10",
    "typescript": "^5.4.0"
  }
}
